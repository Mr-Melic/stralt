/**
 * Copy for a future “return to champion slots” control.
 * Display only — GameFlow still hides Back while currentStage === "world".
 * Do not wire this into GameFlow/WorldExploration from an unattended audit
 * (those files are owned by older open PRs).
 */

export const LEAVE_REALM_LABEL = "Champions";

export const LEAVE_REALM_TITLE =
  "Return to champion slots. Your progress is already saved.";

export const LEAVE_REALM_ARIA = "Leave the realm and return to champion slots";
