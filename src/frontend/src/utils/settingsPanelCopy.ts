/**
 * Sound dock chrome (WorldExploration SettingsPanel).
 * Display only — does not change the sound engine.
 */

/** Folded-tab / header title. The panel only has volume + mute. */
export const SETTINGS_PANEL_TITLE = "Sound";

/** One-line why, under the mute control. */
export const SETTINGS_MUTE_HINT =
  "World and battle SFX. This dock is the only mute — there is no HUD gear.";
