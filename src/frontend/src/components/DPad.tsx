// DPad removed — mouse click is the sole movement input per user preference.
// File kept as a minimal stub so any stale import doesn't break the build.
export type Direction =
  | "up"
  | "down"
  | "left"
  | "right"
  | "upleft"
  | "upright"
  | "downleft"
  | "downright";
