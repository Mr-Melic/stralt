// WeatherCanvas removed — weather system was disabled. File kept as empty stub
// to avoid broken imports in any legacy reference. No animation loops run here.
export type WeatherEffect = "none";
export const ALL_WEATHER_EFFECTS: WeatherEffect[] = [];
export function pickRandomWeather(): WeatherEffect {
  return "none";
}
