# Player-journey UX audit — 2026-09-02

**Auditor:** Player Journey & UX Auditor (`96624677-a486-11f1-a7d1-d6b4613131ce`)  
**Visual source of truth:** `DESIGN.md` (preserved; not edited)  
**HEAD inspected:** `58302bc` (`Merge pull request #258` — GameKey shop)  
**Prior audits:** [`UX_AUDIT_2026-08-31.md`](./UX_AUDIT_2026-08-31.md); ACTION ledger [`ACTION_IDS_2026-09-01.md`](./ACTION_IDS_2026-09-01.md) UX block  
**This run:** third dedicated journey audit. GameKey (#258) is the largest player-facing change since 2026-09-01.

DESIGN.md direction kept: Dofus/Ankama carved-stone, dark slate, gold/crimson, orb vitals, isometric chess pieces. No generic SaaS restyle. Purple leftover-XP fill stays (Dofus XP chrome, `--dofus-xp-color` hue 310) — that is not the gray/purple Tailwind drift called out on Game Over.

Player telemetry: none. Findings are from live UI code + the six player questions at each stage.

Do not restore the dummy GameFlow XP bar (`UX-HUD-DUPLICATE-TOPBAR`).  
Do not re-implement leftover XP (`UX-RECAP-XP-CURVE` is resolved).  
Do not invent a Blood spend path (`UX-BLOOD-DEAD-BAR`).  
Do not rewire `deathGuards` unattended (`UX-DEATH-DUAL-MODAL`).

---

## Journey scorecard

| Stage | What should they do? | What can they do? | Why can't they? | What just happened? | Earn / lose? | Next? | Grade |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| Launch | Read the title, sign in | Sign in; hidden admin triple-click `v1.0` | Footer still says `v1.0` while `APP_VERSION` is `v163` | Version bump can wipe cache and force re-login | Nothing | Internet Identity | C+ |
| Login | Complete II | II popup; on-screen error banner | Failure copy exists | Silent catch is gone | Nothing | Profile name | B |
| Profile | Pick an account name | Name + logout | Length rules used to appear only after submit (hint added this run) | Error banner if save fails | Nothing | Slots | B+ |
| Creation | Forge a piece | Name, piece, colors, rotate | Save title explains Play; still no starting HP/AP/MP row | Toast then back to slots | Nothing | Play | B− |
| Selection | Play a slot | Play / edit / delete / create; leftover XP bar | Empty slots need Create | Slots load | Nothing | World | B |
| Exploration | Walk, find portals, pick Doka | Click-to-move, Items, Buy Doka, heal, rename, Enemies | No first-map teaching; only dungeon portals labeled; Enemies lore lies | Map # increments | Portal +10 XP, ground Doka | Encounter or portal | C |
| Encounter | Accept or decline challenge, close distance | Challenge panel, walk into hostiles | Fail banner used to say only “Failed!” (reason added this run) | Combat starts | Challenge bonus if kept | Combat | B |
| Combat | Spend AP/MP, end turn | Walk / Attack / spells / flee / inspect | Illegal tile clicks still mostly log-only | Turn timer, YOUR TURN | HP / AP / MP | Victory or death | B− |
| Spells | Pick a spell, click a legal tile | 8 slots, book, upgrade | Overworld slots were clickable again after SpellFooter merge (muted this run) | Cooldown number on the slot | AP | Cast or deselect | B− |
| Victory | Read the recap, keep walking | Recap at app root (z-9999) | No LEVEL UP chrome (sound exists) | Recap + toast | XP / Doka / challenges / feats | Explore / shop / dungeon | B− |
| Rewards | Understand the funnel | Recap, feats, heal | Two stores remain (Items vs Buy Doka) | Persist lock (invisible) | Wallet | Spend or fight | C+ |
| Progression | Level, upgrade spells | Spellbook shows Doka cost; summons 10× | Cost is a number, not “10× summon tax” in words | Level badge | Levels, spell ranks | Harder zones | B− |
| Death | Read the cost, go to Death Realm | Combat → Game Over; lava → recap + 1.5s auto-realm | Two beats still disagree | 20% XP / 40% Doka | Penalty | Death Realm | D |
| Recovery | Find a portal | Walk, toast | 1.5s wait has no “you are dead” beat; Death Realm exits unlabeled | Half HP | Nothing more | Portal out | C |
| Shop | Buy potions or credit Doka | Items cluster + Buy Doka GameKey | Cart is icon-only; QR sits above “submit email first” (how-to list added) | Toast; pending/approved chip | Doka / potions | Use or fight | C+ |
| Upgrades | Rank a spell | Spellbook | “Not enough Doka” is good | Level number | Doka | Equip | B− |
| Dungeon | Step on the labeled portal | Nearby dungeon label + depth pill | Other portal kinds have no label | Chain indicator | Multiplier Doka | Next room or death | C+ |
| Boss | Read Bosses, take the purple portal | Guide + banner | Every boss portal is the same purple; rush chip is arcade pink | Boss banner 1.5s | Boss recap | Rush / overworld | C |

---

## Theme findings

### Visual hierarchy

Live HUD is WorldExploration’s top strip (name, level, Map #, leftover XP, region, Doka, cart). GameFlow keeps a pointer-events-none spacer plus an Items / Board / Feats / Bosses cluster at `z-9001` under the HUD. That cluster still collides with the live strip on mid-width tablets (`UX-HUD-TOOL-CLUSTER`).

Boss Rush room chip is still `#FF69B4` arcade pink under the header (`UX-BOSS-RUSH-PINK`). Dungeon depth uses carved crimson. Do not “fix” leftover-XP purple — it matches `--dofus-xp-color` and Dofus XP chrome.

Identity drift (`UX-IDENTITY-FONT-DRIFT`): DESIGN.md asks for Space Grotesk / Inter / OKLCH. Launch title measures Arial; `index.css` is Baloo 2 / Saira. Next new screen only. No repo-wide hex sweep.

### Action discoverability

First map still has no coach (`UX-ONBOARD-FIRST-MAP`). Champion forge still hides starting 100 HP / 10 AP / 5 MP (`UX-CREATE-NO-STATS`). Overworld spell dock looked live after SpellFooter was folded into `BattleUIPanel`; this run disables slots out of battle and restores the fight-first hover.

Buy Doka after #258 is a four-beat off-game loop (email → Mollie → admin approval → 120-char redeem). The QR was visually first while the toast said submit-then-pay. This run adds a carved **How to buy** list; the QR is still above the form (`UX-GAMEKEY-STEP-ORDER` remaining layout).

### Feedback / invalid actions

Cast failures on the canvas still go to the battle log (`UX-CAST-FAIL-FEEDBACK`). Challenge fail now names the broken rule. GameKey wrap/newlines from email now strip before validate. Approval status polls every 8s while the modal is open.

### AP / MP / spell / enemy

Battle orbs still use `maxBattleAp` / `maxBattleMp`. Side-panel jewels still use `vitalsOrbCaps` (`UX-VITALS-ORB-MAX` resolved). Enemy inspect still lives on initiative chips; there is still no persistent “this is the thing you are aiming at” chrome. The world **Enemies** button opens `EnemyRegister` lore the engine does not run (`UX-ENEMY-REGISTER-LORE` / PXA-2026-09-01-001). Do not implement the register as truth.

### Portals / rewards / death / HUD / modals / responsive

Dungeon portals label when nearby; rest / boss / colored / white sanctuary / Death Realm exits do not (`UX-PORTAL-LEGEND`). Recap leftover XP is truthful (`UX-RECAP-XP-CURVE`). Combat death still unmounts the world into Game Over; lava death still fires the root recap and auto-enters the realm in 1.5s (`UX-DEATH-DUAL-MODAL`). `<768px` offers Continue anyway; there is still no stacked HUD (`UX-SMALL-SCREEN-HARD-BLOCK`). Changelog still appears only after II re-login (`UX-VERSION-FORCE-RELOGIN`); this run refreshed the four bullets so the *next* bump is not a 2025 feature list. `APP_VERSION` was **not** bumped (that would force every player through II again).

---

## What this run changed (small, display-only)

1. Buy Doka: numbered how-to, Escape to close, 8s status poll + pending→approved toast, GameKey whitespace strip + `n/120` counter.
2. Overworld spell slots and Attack Nearest muted again (`BattleUIPanel` `inBattle` gate).
3. Challenge fail banner names the broken rule; Striker line says “All casts within 2 tiles”.
4. Profile name field: 2–50 hint + `maxLength`.
5. Changelog bullets match the live loop (GameKey, Items vs Buy Doka, Death Realm, leftover XP). No `APP_VERSION` bump.

Not implemented (need a human): death-flow unification, first-run teaching, portal legend, jewel/HUD cluster merge, two-shop IA, GameKey QR-below-form reorder, 120-char product change, EnemyRegister honesty, small-screen HUD, version-forced re-login, boss-rush pink, Feats vs Achievements naming.

Actionable records: [`ACTION_IDS_UX_2026-09-02.md`](./ACTION_IDS_UX_2026-09-02.md).

---

## Verification

- Unit: `iapShopCopy.test.ts` (how-to steps, wrap-strip GameKey), `challengeCompletion.test.ts` (fail copy).
- `pnpm typecheck` + `pnpm check` (frontend import gate).
- No Motoko / mock / bindgen edits → backend `mops check` not required for this delta.
- Browser: not exercised end-to-end (Internet Identity + live canister). Closest substitute is the copy/helper tests above.
