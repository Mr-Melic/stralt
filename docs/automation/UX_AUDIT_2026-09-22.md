# Player-journey UX audit — 2026-09-22

**Auditor:** Player Journey & UX Auditor (`96624677-a486-11f1-a7d1-d6b4613131ce`)  
**Visual source of truth:** `DESIGN.md` (preserved; not edited)  
**HEAD inspected:** `0f5363f` (`Merge pull request #332`) — same main SHA as the 2026-09-21 audit  
**Prior audits:** [`UX_AUDIT_2026-09-21.md`](https://github.com/Mr-Melic/stralt/blob/cursor/stralt-ux-audit-209c/docs/automation/UX_AUDIT_2026-09-21.md) (open PR #372); [`UX_AUDIT_2026-09-02.md`](./UX_AUDIT_2026-09-02.md); ACTION ledger [`ACTION_IDS_UX_2026-09-02.md`](./ACTION_IDS_UX_2026-09-02.md)

DESIGN.md direction kept: Dofus/Ankama carved-stone, dark slate, gold/crimson, orb vitals, isometric chess pieces. No generic SaaS restyle. Purple leftover-XP fill stays (`--dofus-xp-color` hue 310).

Player telemetry: none. Findings are from live UI code plus the six player questions at each stage.

Do not restore the dummy GameFlow XP bar (`UX-HUD-DUPLICATE-TOPBAR`).  
Do not re-implement leftover XP math (`UX-RECAP-XP-CURVE` is resolved).  
Do not invent a Blood spend path (`UX-BLOOD-DEAD-BAR`).  
Do not rewire `deathGuards` unattended (`UX-DEATH-DUAL-MODAL`).  
Do not bump `APP_VERSION` from a copy pass (forces II re-login).  
Do not duplicate open PR #372’s display hunks (forge name hint, profile alert, Items title, GameKey paste, summon 10× line, challenge Skip bonus).

Stack-compat: older still-open PRs own `WorldExploration.tsx` (#327, #331, #340, …), `GameFlow.tsx` / `BattleUIPanel.tsx` (#335, #340), Feats chrome (#354), ChallengePanel (#364 + #372). This run does **not** edit those files.

---

## Journey scorecard

| Stage | What should they do? | What can they do? | Why can't they? | What just happened? | Earn / lose? | Next? | Grade |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| Launch | Read ÆSTRALTØ, sign in | II CTA; hidden `v1.0` triple-click | Tab title still Paper Baby Vampires; footer `v1.0` vs `APP_VERSION` `v163` | Version bump can wipe cache and force re-login | Nothing | Internet Identity | C+ |
| Login | Complete II | II popup; on-screen error (`role="alert"`) | Authenticating disables the CTA | Failure copy exists | Nothing | Profile name | B |
| Profile | Pick an account name | Name 2–50 + logout | Submit blocked while pending; length shown | Error banner (alert role still queued in #372) | Nothing | Slots | B+ |
| Creation | Forge a piece | Name, piece, colors, rotate, starting HP/AP/MP/INIT | Save title explains empty name / drawing / Play; 20-char cap has no helper on main (#372) | Toast then back to slots | Nothing on forge | Play | B |
| Selection | Play a slot | Play / edit / delete / create; leftover XP this level (this run) | Empty slots need Create | Slots load; heading now “Choose your champion” | Nothing | World | B+ |
| Exploration | Walk, find portals, pick Doka | Click-to-move, Items, Buy Doka, heal, rename, Enemies | No first-map teaching; only dungeon portals labeled; cart is icon-only | Map # increments | Portal +10 XP, ground Doka | Encounter or portal | C |
| Encounter | Accept challenge before first action, close distance | Challenge panel (in-battle offer), walk into hostiles | Unaccepted offer dies on first AP/MP spend; Accept vs Skip copy still queued in #372 | Fail banner names the rule | Challenge bonus if kept | Combat | B− |
| Combat | Spend AP/MP, end turn | Walk / Attack / spells / flee / inspect | Illegal tile clicks: casts often float; walks still log-only | Turn timer, YOUR TURN | HP / AP / MP | Victory or death | B− |
| Spells | Pick a spell, click a legal tile | 8 slots, book, upgrade | Overworld slots muted until fight; 10× summon tax copy queued in #372 | Cooldown number on the slot | AP, Doka on rank-up | Cast or deselect | B |
| Victory | Read the recap, keep walking | Recap at app root (z-9999) | No LEVEL UP chrome (sound exists); Feats vs Achievements wording | Recap + toast | XP / Doka / challenges / feats | Explore / shop / dungeon | B− |
| Rewards | Understand the funnel | Recap, feats, heal | Two stores remain (Items vs Buy Doka cart) | Persist lock (invisible) | Wallet | Spend or fight | C+ |
| Progression | Level, upgrade spells | Spellbook shows Doka cost; summons 10× in math | Cost is a number without “10×” until #372 | Level badge | Levels, spell ranks | Harder zones | B− |
| Death | Read the cost, go to Death Realm | Combat → Game Over (rule always visible this run); lava → recap + 1.5s auto-realm | Two beats still disagree | 20% leftover XP / 40% Doka | Penalty | Death Realm | D+ |
| Recovery | Find a portal | Walk, toast | 1.5s wait has no “you are dead” beat; Death Realm exits unlabeled | Half HP | Nothing more | Portal out | C |
| Shop | Buy potions or credit Doka | Items cluster + Buy Doka GameKey | HUD cart is still icon-only; two ShoppingCart icons; Items modal still “Item Shop” on main (#372) | Toast; pending/approved chip | Doka / potions | Use or fight | B− |
| Upgrades | Rank a spell | Spellbook | “Not enough Doka” is good | Level number | Doka | Equip | B |
| Dungeon | Step on the labeled portal | Nearby dungeon label + depth pill | Other portal kinds have no label; chain-active labels every nearby portal | Chain indicator | Multiplier Doka | Next room or death | C+ |
| Boss | Read Bosses, take the purple portal | Guide + banner | Every boss portal is the same purple; rush chip is arcade pink | Boss banner 1.5s | Boss recap | Rush / overworld | C |

---

## Theme findings

### Visual hierarchy

Live HUD is WorldExploration’s top strip (name, level, Map #, leftover XP, region, Doka, cart). GameFlow pins Items / Board / Feats / Bosses under a spacer at `z-9001`. Challenge panel is `z-index: 1200`, so the tool cluster can paint over it on a crowded tablet (`UX-HUD-TOOL-CLUSTER`, `UX-CHALLENGE-ZINDEX`).

Boss Rush room chip is still `#FF69B4` arcade pink (`UX-BOSS-RUSH-PINK`, WorldExploration ~17586). Not restyled — `WorldExploration.tsx` is already in older open PRs.

Identity drift (`UX-IDENTITY-FONT-DRIFT`): DESIGN.md asks for Space Grotesk / Inter / OKLCH. Live CSS is Baloo 2 / Saira (`index.css` `--font-display`). Next new screen only. No repo-wide hex sweep.

Selection heading no longer shouts the leftover “Paper Baby Vampire” nickname (`UX-SELECTION-PBV-HEADING`). GameFlow’s select/forge wordmark and the browser tab still say Paper Baby Vampires (`UX-LAUNCH-TAB-TITLE`).

### Action discoverability

First map still has no coach (`UX-ONBOARD-FIRST-MAP`). Champion forge already shows starting vitals on main. Play on a filled slot now says it enters the realm. Overworld spells stay muted until `inBattle`.

Buy Doka after GameKey is email → Mollie → admin approval → 120-char redeem. HUD cart is still icon-only next to an Items button that also uses `ShoppingCart` (`UX-SHOP-TWO-STORES`).

Challenge offer is **in-battle**, until the first action. Accept-window copy is still #372, not this PR.

### Feedback / invalid actions

Cast failures on the canvas often spawn float text. Walk “Can't reach” / “Not enough MP” still go to the battle log (`UX-CAST-FAIL-FEEDBACK`). Slot-load failure now uses `role="alert"` (`UX-SELECTION-ERROR-ALERT`). Game Over always prints the 20/40 rule even when the lost amounts are zero (`UX-GAMEOVER-PENALTY-COPY`).

### AP / MP / spell / enemy

Battle orbs show current AP/MP plus a 30px mini bar, not `current / max` numerals (`UX-AP-MP-CURRENT-MAX`, BattleUIPanel ~412). Side-panel jewels still use `vitalsOrbCaps`. Enemy inspect lives on initiative chips; Enemies register is labeled flavor lore.

### Portals / rewards / death / HUD / modals / responsive

Dungeon portals label when nearby; rest / boss / colored / white sanctuary / Death Realm exits do not (`UX-PORTAL-LEGEND`, WorldExploration ~7916). When `dungeonChainActive`, the nearby-label branch is `p.color === "dungeon" || dungeonChainActive`, so **non-dungeon** portals can inherit “Continue Chain” copy. Recap leftover XP is truthful. Combat death still unmounts into Game Over; lava death still fires the root recap and auto-enters the realm in 1.5s (`UX-DEATH-DUAL-MODAL`). Recap still has no LEVEL UP chrome (`UX-LEVEL-UP-CHROME`). `<768px` offers Continue anyway (`UX-SMALL-SCREEN-HARD-BLOCK`). Changelog still appears only after II re-login (`UX-VERSION-FORCE-RELOGIN`). `APP_VERSION` remains `v163`.

---

## What this run changed (small, display-only)

1. Character select heading: “Choose your champion” (carved crimson/gold gradient kept).
2. Slot XP reads “N / N this level” with a leftover-XP hover.
3. Play is named “enter the realm” (title + aria-label).
4. Slot-load errors use `role="alert"`.
5. Game Over always states 20% leftover XP / 40% Doka; amount chips drop the duplicate percent when a loss actually posted.

Not implemented (need a human, or blocked by older PRs / already in #372): death-flow unification, first-run teaching, portal legend, Buy Doka HUD label, Boss Rush pink, jewel/HUD cluster merge, AP/MP `n/max` numerals, small-screen HUD, version-forced re-login, Feats vs Achievements (#354), landing `v1.0` visible string, `index.html` / GameFlow tab wordmark, LEVEL UP recap chrome, #372’s forge/Items/GameKey/challenge copy.

Actionable records: [`ACTION_IDS_UX_2026-09-22.md`](./ACTION_IDS_UX_2026-09-22.md).

---

## Verification

- Unit: `characterSelectionCopy.test.ts`, `gameOverModalCopy.test.ts`.
- `bash scripts/caffeine-import-gate.sh frontend` (duplicate-export scan, `pnpm typecheck`, `pnpm check`, `vite build`).
- `bash scripts/open-pr-stack-compat.sh --self` after commit.
- No Motoko / mock / bindgen edits → backend `mops check` not required for this delta.
- Browser (Vite `http://127.0.0.1:5173`, no canister ID in this VM):
  - Desktop ~1280: landing is carved-stone / crimson / starfield, title ÆSTRALTØ, CTA “Sign In with Internet Identity” / “ENTER THE REALM”. Not generic SaaS.
  - Tab title is still “Paper Baby Vampires” (`UX-LAUNCH-TAB-TITLE`).
  - Sign In click cannot complete II here (`CANISTER_ID_BACKEND is not set`); console errors, no on-page login_error in this env (`login()` does not throw).
  - Footer `v1.0` is ~6% opacity camouflage (DOM still has `landing.admin_trigger`).
  - Resize to 390 after a 1280 load did **not** show “Best on Larger Screens” (`sessionStorage` already set). A cold load at 390 still hits `SmallScreenGuard`.
  - Selection / Game Over / combat / shop were not exercised (Internet Identity + live canister). Copy is locked by `characterSelectionCopy.test.ts` and `gameOverModalCopy.test.ts`.
